'use strict';

function businessTrip(graph, cityNames) {
    let cost = 0;

    for(let i = 0; i < cityNames.getNeighbors(cityNames[i]).some((city) => {
        if (cityNames[i =1]) {
            return city.vertex.value === cities [i + 1].value
        }
    })

    if (contains) {
        let directFlight = graph.getNeighbors(cities[i]).filter((city) => {
          return city.vertex.value === cities[i + 1].value
        })[0]
        cost += nonStop.weight
    }


    return cost
}

module.exports= businessTrip