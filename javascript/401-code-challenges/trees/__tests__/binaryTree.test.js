'use strict';


const { Node, BinaryTree } = require('../binaryTree.js')


describe('Binary Tree', () => {

    it('works', () => {
        expect(true).toBeTruthy();
    });

    xit('Can successfully instantiate an empty tree', () => {

    });

    xit('Can successfully instantiate a tree with a single root node', () => {

    });

    xit('Can successfully add a left child and right child to a single root node', () => {

    });

    xit('Can successfully return a collection from a preorder traversal', () => {

    });

    xit('Can successfully return a collection from an inorder traversal', () => {

    });

    xit('Can successfully return a collection from a postorder traversal', () => {

    });

});